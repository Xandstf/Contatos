package br.edu.ifsp.scl.ads.pdm.contatos;

// Que vai ser implementado pela Activity para tratar dos eventos
// de click em cada célula
public interface OnContatoClickListener {
    void onContatoClick(int posicao);
}
