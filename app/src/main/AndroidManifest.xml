<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.ifsp.scl.ads.pdm.contatos">

    <!-- Permissões de versões abaixo de M eram feitas direto no Manifest -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <!-- Fim das permissões -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Contatos">
        <activity android:name=".ContatosActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ContatoActivity" />
    </application>

</manifest>